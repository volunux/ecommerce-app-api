import { CountryRepositoryImpl } from './country-repository-impl';

describe('CountryRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new CountryRepositoryImpl()).toBeDefined();
  });
});
