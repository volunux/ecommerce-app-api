import { CountryValidationMessages } from './country.validation.messages';

describe('CountryValidationMessages', () => {
  it('should be defined', () => {
    expect(new CountryValidationMessages()).toBeDefined();
  });
});
