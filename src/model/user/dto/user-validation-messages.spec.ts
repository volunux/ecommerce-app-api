import { UserValidationMessages } from './user-validation-messages';

describe('UserValidationMessages', () => {
  it('should be defined', () => {
    expect(new UserValidationMessages()).toBeDefined();
  });
});
