import { UserStatusRepositoryImpl } from './user-status-repository-impl';

describe('UserStatusRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new UserStatusRepositoryImpl()).toBeDefined();
  });
});
