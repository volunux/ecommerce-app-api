import { UserStatusUpdateDto } from './user-status-update.dto';

describe('UserStatusUpdateDto', () => {
  it('should be defined', () => {
    expect(new UserStatusUpdateDto()).toBeDefined();
  });
});
