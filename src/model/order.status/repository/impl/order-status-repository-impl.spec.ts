import { OrderStatusRepositoryImpl } from './order-status-repository-impl';

describe('OrderStatusRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new OrderStatusRepositoryImpl()).toBeDefined();
  });
});
