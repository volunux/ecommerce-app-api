import { OrderStatusServiceImpl } from './order-status-service-impl';

describe('OrderStatusServiceImpl', () => {
  it('should be defined', () => {
    expect(new OrderStatusServiceImpl()).toBeDefined();
  });
});
