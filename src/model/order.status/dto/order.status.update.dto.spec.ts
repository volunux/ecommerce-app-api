import { OrderStatus.Update.Dto } from './order.status.update.dto';

describe('OrderStatus.Update.Dto', () => {
  it('should be defined', () => {
    expect(new OrderStatus.Update.Dto()).toBeDefined();
  });
});
