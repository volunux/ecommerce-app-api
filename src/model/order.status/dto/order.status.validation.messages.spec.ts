import { OrderStatus.Validation.Messages } from './order.status.validation.messages';

describe('OrderStatus.Validation.Messages', () => {
  it('should be defined', () => {
    expect(new OrderStatus.Validation.Messages()).toBeDefined();
  });
});
