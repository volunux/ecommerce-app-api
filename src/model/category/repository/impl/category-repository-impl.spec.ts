import { CategoryRepositoryImpl } from './category-repository-impl';

describe('CategoryRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new CategoryRepositoryImpl()).toBeDefined();
  });
});
