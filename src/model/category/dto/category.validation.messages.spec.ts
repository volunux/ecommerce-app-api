import { CategoryValidation.Messages } from './category.validation.messages';

describe('CategoryValidation.Messages', () => {
  it('should be defined', () => {
    expect(new CategoryValidation.Messages()).toBeDefined();
  });
});
