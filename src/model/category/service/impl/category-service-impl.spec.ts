import { CategoryServiceImpl } from './category-service-impl';

describe('CategoryServiceImpl', () => {
  it('should be defined', () => {
    expect(new CategoryServiceImpl()).toBeDefined();
  });
});
