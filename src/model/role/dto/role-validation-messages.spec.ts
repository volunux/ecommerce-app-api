import { RoleValidationMessages } from './role-validation-messages';

describe('RoleValidationMessages', () => {
  it('should be defined', () => {
    expect(new RoleValidationMessages()).toBeDefined();
  });
});
