import { ProductReviewRepositoryImpl } from './product-review-repository-impl';

describe('ProductReviewRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new ProductReviewRepositoryImpl()).toBeDefined();
  });
});
