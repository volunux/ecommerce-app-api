import { ProductReview.Validation.Messages } from './product.review.validation.messages';

describe('ProductReview.Validation.Messages', () => {
  it('should be defined', () => {
    expect(new ProductReview.Validation.Messages()).toBeDefined();
  });
});
