import { HttpErrorFilterFilter } from './http-error-filter.filter';

describe('HttpErrorFilterFilter', () => {
  it('should be defined', () => {
    expect(new HttpErrorFilterFilter()).toBeDefined();
  });
});
