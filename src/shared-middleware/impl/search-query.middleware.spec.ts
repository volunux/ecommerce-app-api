import { SearchQueryMiddleware } from './search-query.middleware';

describe('SearchQueryMiddleware', () => {
  it('should be defined', () => {
    expect(new SearchQueryMiddleware()).toBeDefined();
  });
});
